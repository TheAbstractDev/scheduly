{{#if jobs}}
  <h1>Webhooks</h1>
  <button class="removeAll">Remove all jobs</button>
  <div id="jobs">
      <table>
        <tr>
          <th>URL</th>
          <th>Status</th>
          <th>Next Run</th>
          {{#each jobs}}
            {{#if lastRunAt}}
              <th>Last Run</th>
            {{/if}}
            {{#if lastFinishedAt}}
              <th>Last Finished</th>
            {{/if}}
          {{/each}}
          <th>Remove</th>
        </tr>
      {{#each jobs}}
        <tr>
          <td class="url">{{url}}</td>
          <td class="status">{{status}}</td>
          <td class="next">{{nextRunAt}}</td>
          {{#if lastRunAt}}
            <td class="last-run">{{lastRunAt}}</td>
          {{/if}}
          {{#if lastFinishedAt}}
            <td class="last-finished">{{lastFinishedAt}}</td>
          {{/if}}
          <td><button class="remove" data-name="{{name}}">Remove</button></td>
        </tr>
      {{/each}}
      </table>
  </div>
{{else}}
  <h2 class="title">{{title}}</h2>
{{/if}}

<h1>Create a job</h1>
<input type="text" class="url" placeholder="url" />
<input type="text" class="scheduling" placeholder="scheduling" />
<textarea class="body" placeholder="body (JSON)"></textarea>
<button class="create" >Create</button>